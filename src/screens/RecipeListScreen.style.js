import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    marginHorizontal: 16,
  },
  categoryContainer: {
    marginTop: 22,
  },
  catgoriesText: {
    fontSize: 22,
    fontWeight: 'bold',
  },
});
